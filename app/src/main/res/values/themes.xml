<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KiaScreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="tabStyle">@style/AppTheme.TabLayout</item>
    </style>

    <style name="titleToolbar" parent="Widget.MaterialComponents.Toolbar">
        <item name="titleTextAppearance">@style/My_toolbar_textAppearance</item>
    </style>

    <style name="My_toolbar_textAppearance" parent="@style/TextAppearance.MaterialComponents.Headline6">
        <item name="fontFamily">@font/kiamedium</item>
        <item name="android:fontFamily">@font/kiamedium</item>
        <item name="android:textSize">@dimen/toolbar_title_size</item>
    </style>

    <style name="AppTheme.TabLayout" parent="Widget.Design.TabLayout">
        <item name="tabSelectedTextColor">@android:color/black</item>
        <item name="tabTextColor">@android:color/darker_gray</item>
        <item name="tabIndicatorColor">@android:color/black</item>
        <item name="tabRippleColor">@android:color/transparent</item>
    </style>

    <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/black</item>
    </style>

    <style name="EditTextGreyStyle" parent="Widget.Design.TextInputLayout"> // important : parent 를 이것으로.
        <item name="colorControlNormal">@color/black</item>  // unFocus 상태일때 밑줄 색.
        <item name="boxStrokeColor">@color/black</item> // Focus 상태일때 밑줄 색.
        <item name="android:textCursorDrawable">@color/black</item>  // cursor 색.
        <item name="helperTextTextColor">@color/black</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_inset</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <item name="postSplashScreenTheme">@style/Theme.KiaScreen</item>

    </style>
</resources>